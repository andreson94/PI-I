package ProjetoQuiz;

import java.util.Random;
import java.util.Scanner;
import javax.swing.JOptionPane;

public class QuizTeste {
   /* static int aleatorio1(){
     int numero = 0;
        int[] num = new int[5];
        Random r = new Random();
        for(int i=0; i<num.length; i++){
             numero = r.nextInt(20) + 1;
             for(int j=0; j<num.length; j++){
                   if(numero == num[j] && j != i){
                         numero = r.nextInt(100) + 1;
                   }else{
                        num[i] = numero;
                   }
             }
        }
       
        return numero;
}
    static void alternativas(){
    Object[] itens = ( “A”, “B”, “C”,"D" };
      Object selectedValue = JOptionPane.showInputDialog(null,
          “Escolha a opção”, “Opçao”,
              JOptionPane.INFORMATION_MESSAGE, null,
                  itens, itens [0]);

    static void opcaoContinue(SIM,NÃO){
   
    return;
}      */        
    public static void main(String[] args) {
        
    /*
            Scanner leitor = new Scanner (System.in);
            String alternativa = null ;
             
            int  pergunta = 0, questao = 0, tentativas = 3 , pontos = 0 ;
            Random aleatorio = new Random();
            boolean resposta = false;
            questao = aleatorio.nextInt(20); 
          
                  switch (questao) {
            case 1:
                JOptionPane.showMessageDialog(null, "quanto da 5+5/5?"
                        + "a " + valor
                        + "\n-> Valor final: " + (valor * 1.1));
                break;
            case 2:
                JOptionPane.showMessageDialog(null, "O valor final do produto receberá aumento é de 15%."
                        + "\n-> Valor inicial: " + valor
                        + "\n-> Valor final: " + (valor * 1.15));
                break;
            case 3:
                JOptionPane.showMessageDialog(null, "O valor final do produto receberá aumento é de 20%."
                        + "\n-> Valor inicial: " + valor
                        + "\n-> Valor final: " + (valor * 1.2));
                break;
            case 4:
                JOptionPane.showMessageDialog(null, "O valor final do produto receberá aumento é de 25%."
                        + "\n-> Valor inicial: " + valor
                        + "\n-> Valor final: " + (valor * 1.25));
                break;
            case 5:
                JOptionPane.showMessageDialog(null, "O valor final do produto receberá aumento é de 30%."
                        + "\n-> Valor inicial: " + valor
                        + "\n-> Valor final: " + (valor * 1.3));
                break;
            default:
                JOptionPane.showMessageDialog(null, "O código informado é inválido.");
                break;
                */    
     //Int continue = JOptionPane.showConfirmDialog(null,"DESEJA CONTINUAR?");       
    }
    }
